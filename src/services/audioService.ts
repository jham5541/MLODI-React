// Use the Expo-compatible audio service for Expo Go
import { Song } from '../types/music';

// Import the Expo-compatible service
export { audioService } from './audioService.expo';
export { State } from './audioService.expo';
export default audioService;